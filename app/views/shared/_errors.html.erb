

<% if obj.errors.any? %>
    <h2>the following errors prevented the <%= obj.class.name.upcase %> from being saved: </h2>
    <ul>
     <% obj.errors.full_messages.each do |msg|%>
      <li><%= msg %></li>
      <% end%>
    </ul>
<%end%>